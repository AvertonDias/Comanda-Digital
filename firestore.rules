rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // USERS: Perfil do usuário
    match /users/{userId} {
      allow read, write: if isSignedIn();
    }

    // RESTAURANTS: Acesso total para usuários autenticados no protótipo
    match /restaurants/{restaurantId} {
      allow read, write: if isSignedIn();
      
      // Corrigido para garantir que listagens de coleções e subcoleções funcionem
      match /{allPaths=**} {
        allow read, write: if isSignedIn();
      }
    }
  }
}